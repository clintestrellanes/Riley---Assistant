import React from 'react'

interface CreateCalendarProps {
  onClose: () => void;
}

export default function CreateCalendar({ onClose }: CreateCalendarProps) {
  return (
    <div> 
      <h1>CreateCalendar</h1>
    </div>
  )
}